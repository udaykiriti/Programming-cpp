version: '3.8'

services:
  programming-cpp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: programming-cpp-dev
    volumes:
      - .:/workspace
    working_dir: /workspace
    command: /bin/bash
    stdin_open: true
    tty: true
