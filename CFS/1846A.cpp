#include <bits/stdc++.h>
using namespace std;

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0); cout.tie(0);

  int t; cin >> t;
  while (t--) {
    int n;
    cin >> n;
    int t = n; int c = 0;
    int a, b;
    while (n--){
      cin >> a >> b;
      if (b >= a c++;
    }
    cout << t - c << endl;
  } return 0;
}